services:
  # Web Service
  - type: web
    name: neurochat
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 18
    healthCheckPath: /api/health
    autoDeploy: true
